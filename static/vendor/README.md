# Three.js

Vendored from npm `three@0.186.0` with `npm run vendor`. Files are unmodified.
The upstream MIT license is included in `three/LICENSE`.
Upstream: https://github.com/mrdoob/three.js

`package-lock.json` pins the dependency and download integrity. Runtime modules are
served locally so the viewer does not depend on a third-party CDN.
