# jikhanjung 057 — v0.10.0 배포와 NAS 백업

날짜: 2026-09-14

## 한 것

- v0.10.0 배포. 담긴 것: 선 레이어가 3D 지형을 따라감(#23, 053), 기울기·방향 조작(#24, 054),
  오버레이 겹침 정리와 번역 파일 검사(#25, 055), 라이선스(#26, 056). 자료 묶음은 v0.9.4와 같다.
- 하루 한 번 NAS 백업. 정본은 `system-operation/m710q/backup-earththrutime.sh`(cron 04:20)이고,
  fsis2026 의 `backup-fsis.sh` 구조를 따랐다.
  - 이 프로젝트는 자료가 DB 가 아니라 파일이고 개발 호스트에서 만들어 서버에 올린다. 그래서 fsis 와
    방향이 반대다: dolfinid 에서 당기는 것은 검증된 매시 DB 스냅샷(관리자 계정뿐)과 `.env.django`
    이고, NAS 로 미러하는 것은 개발 호스트의 `data/sources`(3.6 GB)·`data/derived`(158 MB)다.
  - DB 스냅샷은 `PRAGMA integrity_check` 를 통과해야 채택하고, 로컬 30일·NAS 90일 뒤 월초와
    12월 1일만 남긴다. 최신 릴리스 묶음 하나도 NAS 에 둬 개발 호스트 없이 재배포할 수 있다.
  - 첫 실행 1분, NAS 3.9 GB. 실패하면 텔레그램으로 알린다.
- `docs/operations.md` 의 남은 항목에서 원격지 백업을 뺐다. 복구 리허설은 DB 에 지킬 것이 생길
  때까지 미룬다고 적었다.
