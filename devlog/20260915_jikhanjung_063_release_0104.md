# jikhanjung 063 — v0.10.4: 최근 2.5만 년 시간 범위

날짜: 2026-09-15

## 한 것

- #31(jikhanjung 062, 계획 P01)을 main 에 합쳤다. 고도 격자 시리즈에 "시간 범위: 최근 2.5만 년"이
  생겨 25 ka부터 현재까지 1천 년 간격으로 빙하와 해수면을 볼 수 있다.
- 자료 묶음에는 저수위 조각이 25장 들어간다. v0.10.3 은 18장이었다.
- v0.10.4 배포.

## 확인

- 브랜치에서 `make check`, `make test` 52개, `tests/ice_check.py` 7개, `npm test` 13개, 브라우저 검사
  전체가 통과했다(062). 머지는 충돌 없이 됐고 main 에 다른 커밋은 없었다.
- `build.sh`: 이미지 검사 통과(0.10.4, 거리장 90장, 판 모델 5개). 자료 묶음에 저수위 조각 25장이 들어갔다.
- 배포 뒤 `/healthz` 0.10.4, 서버 검사 통과(묶음 616개 파일 검증), 배포 직전 DB 스냅샷
  `db-20260915T050621Z.sqlite3`.
- 공개 주소:
  - `?masks=paleodem2018&window=deglacial` 에 범위 선택이 있고 "최근 2.5만 년"이 골라져 있다.
  - 프레임 26개.
  - 새로 들어간 조각 `/globe/ice-low/paleodem-0000/3.png`·`25.png`, 그리고 `21.png` 가 200 이다.
- prune 으로 v0.10.2 를 지우고 v0.10.4·v0.10.3 만 남겼다(323 MiB 회수, 서버 여유 13.7 GiB).
